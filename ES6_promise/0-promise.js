export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // You can resolve or reject the promise here based on some conditions.
    // For this task, we don't need to specify any conditions.
  });
}
